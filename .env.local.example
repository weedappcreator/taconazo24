# Update these anytime from your Vercel dashboard — no code changes needed
NEXT_PUBLIC_GOOGLE_RATING=4.9
NEXT_PUBLIC_GOOGLE_REVIEWS=7319

# Optional: for live data from Google Places API
# 1. Enable Places API (new) at https://console.cloud.google.com
# 2. Create an API key, restrict by HTTP referrer
# 3. Add the key below (falls back to env vars above if unset)
GOOGLE_PLACES_API_KEY=
